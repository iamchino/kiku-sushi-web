const StickyCTA = () => {
  return (
    <a
      href="#reservar"
      className="md:hidden fixed bottom-4 left-4 right-4 z-50 bg-gradient-neon text-primary-foreground text-center font-semibold tracking-wider uppercase py-4 rounded-2xl glow-neon-strong animate-pulse-glow text-sm"
    >
      Reservar mesa ✦
    </a>
  );
};

export default StickyCTA;
